# ui/src/app/api/auth — Auth API Routes

Server-side OAuth flows for external integrations.

| Directory | Purpose |
|-----------|---------|
| `jira/` | Jira OAuth flow (initiate, complete, status, refresh, disconnect, token) |
